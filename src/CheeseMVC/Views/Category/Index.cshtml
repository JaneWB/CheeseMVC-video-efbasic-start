@model List<CheeseMVC.Models.CheeseCategory>

@{
    ViewData["Title"] = "Index";
 }

<h2>Categories</h2>

<ul>
    @foreach (var category in Model)
    {
        <li>@category.Name</li>
    }
</ul>

<p><a asp-controller="Category" asp-action="Add">Add a Category of cheese</a></p>